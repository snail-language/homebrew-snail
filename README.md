# Snail-language Snail

## How do I install these formulae?

`brew install snail-language/snail/<formula>`

Or `brew tap snail-language/snail` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
